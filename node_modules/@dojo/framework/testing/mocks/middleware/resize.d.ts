import { DefaultMiddlewareResult } from '../../../core/interfaces';
export declare function createResizeMock(): {
    (): DefaultMiddlewareResult;
    (key: string, contentRect: Partial<DOMRectReadOnly>): void;
};
export default createResizeMock;
