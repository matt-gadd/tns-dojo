import { StatePaths } from '../../../stores/Store';
import { DefaultMiddlewareResult } from '../../../core/interfaces';
import { PatchOperation } from '../../../stores/state/Patch';
import { Process } from '../../../stores/process';
export declare function createMockStoreMiddleware<T = any>(processes?: [Process<any, any>, any][]): {
    (): DefaultMiddlewareResult;
    (operations: (path: StatePaths<T>) => PatchOperation<any, any>[]): void;
};
export default createMockStoreMiddleware;
