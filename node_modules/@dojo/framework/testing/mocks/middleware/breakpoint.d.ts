import { Breakpoints } from '../../../core/middleware/breakpoint';
import { DefaultMiddlewareResult } from '../../../core/interfaces';
export declare function createBreakpointMock(breakpoints?: Breakpoints): {
    (): DefaultMiddlewareResult;
    (key: string, breakpointResult: {
        breakpoint: string;
        contentRect: Partial<DOMRectReadOnly>;
    }): void;
};
export default createBreakpointMock;
