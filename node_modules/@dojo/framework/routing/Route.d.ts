import { DNode } from '../core/interfaces';
import { MatchDetails } from './interfaces';
export interface RouteProperties {
    renderer: (matchDetails: MatchDetails) => DNode | DNode[];
    id: string;
    routerKey?: string;
}
export declare const Route: import("../core/interfaces").DefaultChildrenWNodeFactory<{
    properties: RouteProperties & import("../core/interfaces").WidgetProperties;
    children: DNode<any>[];
}>;
export default Route;
