(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "../core/vdom", "../core/middleware/injector", "../core/middleware/icache"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var vdom_1 = require("../core/vdom");
    var injector_1 = require("../core/middleware/injector");
    var icache_1 = require("../core/middleware/icache");
    var factory = vdom_1.create({ icache: icache_1.default, injector: injector_1.default, diffProperty: vdom_1.diffProperty, invalidator: vdom_1.invalidator }).properties();
    exports.Route = factory(function Route(_a) {
        var _b = _a.middleware, icache = _b.icache, injector = _b.injector, diffProperty = _b.diffProperty, invalidator = _b.invalidator, properties = _a.properties;
        var _c = properties(), renderer = _c.renderer, id = _c.id, _d = _c.routerKey, routerKey = _d === void 0 ? 'router' : _d;
        var currentHandle = icache.get('handle');
        if (!currentHandle) {
            var handle_1 = injector.subscribe(routerKey);
            if (handle_1) {
                icache.set('handle', function () { return handle_1; });
            }
        }
        diffProperty('routerKey', function (current, next) {
            var _a = current.routerKey, currentRouterKey = _a === void 0 ? 'router' : _a;
            var _b = next.routerKey, routerKey = _b === void 0 ? 'router' : _b;
            if (routerKey !== currentRouterKey) {
                var currentHandle_1 = icache.get('handle');
                if (currentHandle_1) {
                    currentHandle_1();
                }
                var handle_2 = injector.subscribe(routerKey);
                if (handle_2) {
                    icache.set('handle', function () { return handle_2; });
                }
            }
            invalidator();
        });
        var router = injector.get(routerKey);
        if (router) {
            var routeContext = router.getRoute(id);
            if (routeContext) {
                var queryParams = routeContext.queryParams, params = routeContext.params, type = routeContext.type, isError = routeContext.isError, isExact = routeContext.isExact;
                var result = renderer({ queryParams: queryParams, params: params, type: type, isError: isError, isExact: isExact, router: router });
                if (result) {
                    return result;
                }
            }
        }
        return null;
    });
    exports.default = exports.Route;
});
//# sourceMappingURL=Route.js.map